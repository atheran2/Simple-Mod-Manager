<Window x:Class="VintageStoryModManager.Views.Dialogs.CreateInstanceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New Instance"
        Width="520"
        Height="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False"
        Loaded="Window_OnLoaded"
        Background="{DynamicResource Brush.Window.Shell.Background.Solid}">
    <Grid Margin="20"
          Background="{DynamicResource Brush.Panel.Primary.Background.Solid}"
          TextElement.Foreground="{DynamicResource Brush.Text.Primary}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Instance Name -->
        <TextBlock Text="Name:"
                   Grid.Row="0" Grid.Column="0"
                   Margin="0,0,12,12"
                   VerticalAlignment="Center" />
        <TextBox x:Name="NameTextBox"
                 Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
                 Margin="0,0,0,12"
                 TextChanged="NameTextBox_OnTextChanged"
                 Background="{DynamicResource Brush.Dialog.TextBox.Background}"
                 BorderBrush="{DynamicResource Brush.Dialog.TextBox.Border}"
                 SelectionBrush="{DynamicResource Brush.Dialog.TextBox.SelectionBackground}"
                 SelectionTextBrush="{DynamicResource Brush.Dialog.TextBox.SelectionForeground}"
                 CaretBrush="{DynamicResource Brush.Dialog.TextBox.Caret}" />

        <!-- Game Directory -->
        <TextBlock Text="Game Directory:"
                   Grid.Row="1" Grid.Column="0"
                   Margin="0,0,12,12"
                   VerticalAlignment="Center" />
        <TextBox x:Name="GameDirectoryTextBox"
                 Grid.Row="1" Grid.Column="1"
                 Margin="0,0,8,12"
                 IsReadOnly="True"
                 Background="{DynamicResource Brush.Dialog.TextBox.Background}"
                 BorderBrush="{DynamicResource Brush.Dialog.TextBox.Border}"
                 SelectionBrush="{DynamicResource Brush.Dialog.TextBox.SelectionBackground}"
                 SelectionTextBrush="{DynamicResource Brush.Dialog.TextBox.SelectionForeground}"
                 CaretBrush="{DynamicResource Brush.Dialog.TextBox.Caret}" />
        <Button x:Name="BrowseButton"
                Grid.Row="1" Grid.Column="2"
                Content="..."
                Width="30"
                Margin="0,0,0,12"
                Click="BrowseButton_OnClick"
                ToolTip="Browse for game directory or executable" />

        <!-- Detected Version -->
        <TextBlock Text="Detected Version:"
                   Grid.Row="2" Grid.Column="0"
                   Margin="0,0,12,12"
                   VerticalAlignment="Center" />
        <TextBox x:Name="VersionTextBox"
                 Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                 Margin="0,0,0,12"
                 Background="{DynamicResource Brush.Dialog.TextBox.Background}"
                 BorderBrush="{DynamicResource Brush.Dialog.TextBox.Border}"
                 SelectionBrush="{DynamicResource Brush.Dialog.TextBox.SelectionBackground}"
                 SelectionTextBrush="{DynamicResource Brush.Dialog.TextBox.SelectionForeground}"
                 CaretBrush="{DynamicResource Brush.Dialog.TextBox.Caret}"
                 ToolTip="Auto-detected from game directory. You can also type a version manually." />

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Grid.Column="0" Grid.ColumnSpan="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,18,0,0">
            <Button x:Name="CreateButton"
                    Content="Create"
                    MinWidth="88"
                    Margin="0,0,12,0"
                    IsDefault="True"
                    IsEnabled="False"
                    Click="CreateButton_OnClick" />
            <Button Content="Cancel"
                    MinWidth="88"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
